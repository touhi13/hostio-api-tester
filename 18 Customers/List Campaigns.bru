meta {
  name: List Campaigns
  type: http
  seq: 9
}

get {
  url: {{base_url}}/api/v1/restaurants/{{restaurant_id}}/campaigns
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}
