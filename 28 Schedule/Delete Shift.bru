meta {
  name: Delete Shift
  type: http
  seq: 7
}

delete {
  url: {{base_url}}/api/v1/restaurants/{{restaurant_id}}/schedule/{{scheduled_shift_id}}
  body: none
  auth: none
}

headers {
  Accept: application/json
  Authorization: Bearer {{token}}
}
