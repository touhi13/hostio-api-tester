meta {
  name: Statement Ratios
  type: http
  seq: 16
}

get {
  url: {{base_url}}/api/v1/restaurants/{{restaurant_id}}/accounting/statements/ratios
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}
