meta {
  name: Show Invoice
  type: http
  seq: 27
}

get {
  url: {{base_url}}/api/v1/restaurants/{{restaurant_id}}/accounting/invoices/{{invoice_id}}
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}
