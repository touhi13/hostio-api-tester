meta {
  name: Account Ledger
  type: http
  seq: 7
}

get {
  url: {{base_url}}/api/v1/restaurants/{{restaurant_id}}/accounting/accounts/{{account_id}}/ledger
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}
