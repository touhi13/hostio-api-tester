meta {
  name: Show Account
  type: http
  seq: 5
}

get {
  url: {{base_url}}/api/v1/restaurants/{{restaurant_id}}/accounting/accounts/{{account_id}}
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}
