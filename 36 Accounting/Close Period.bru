meta {
  name: Close Period
  type: http
  seq: 40
}

post {
  url: {{base_url}}/api/v1/restaurants/{{restaurant_id}}/accounting/periods/{{period_id}}/close
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}
