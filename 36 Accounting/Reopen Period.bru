meta {
  name: Reopen Period
  type: http
  seq: 41
}

post {
  url: {{base_url}}/api/v1/restaurants/{{restaurant_id}}/accounting/periods/{{period_id}}/reopen
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}
