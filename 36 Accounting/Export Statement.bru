meta {
  name: Export Statement
  type: http
  seq: 17
}

get {
  url: {{base_url}}/api/v1/restaurants/{{restaurant_id}}/accounting/statements/{{statement_key}}/export?format=csv
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}
