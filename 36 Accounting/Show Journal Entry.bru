meta {
  name: Show Journal Entry
  type: http
  seq: 10
}

get {
  url: {{base_url}}/api/v1/restaurants/{{restaurant_id}}/accounting/journal/{{journal_id}}
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}
