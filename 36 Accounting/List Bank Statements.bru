meta {
  name: List Bank Statements
  type: http
  seq: 30
}

get {
  url: {{base_url}}/api/v1/restaurants/{{restaurant_id}}/accounting/bank-statements
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}
