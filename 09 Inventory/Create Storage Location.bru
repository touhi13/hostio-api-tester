meta {
  name: Create Storage Location
  type: http
  seq: 14
}

post {
  url: {{base_url}}/api/v1/restaurants/{{restaurant_id}}/inventory/storage-locations
  body: json
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
      "name": "Wine Cellar"
  }
}
