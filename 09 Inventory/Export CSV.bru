meta {
  name: Export CSV
  type: http
  seq: 8
}

get {
  url: {{base_url}}/api/v1/restaurants/{{restaurant_id}}/inventory/export
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}
