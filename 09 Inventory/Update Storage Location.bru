meta {
  name: Update Storage Location
  type: http
  seq: 17
}

put {
  url: {{base_url}}/api/v1/restaurants/{{restaurant_id}}/inventory/storage-locations/{{storage_id}}
  body: json
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
      "name": "Walk-in Cooler A",
      "description": "Primary cold storage"
  }
}
