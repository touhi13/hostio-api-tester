meta {
  name: Remove Staff
  type: http
  seq: 6
}

delete {
  url: {{base_url}}/api/v1/restaurants/{{restaurant_id}}/staff/{{staff_id}}
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}
