meta {
  name: List Transactions
  type: http
  seq: 12
}

get {
  url: {{base_url}}/api/v1/restaurants/{{restaurant_id}}/transactions
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}
