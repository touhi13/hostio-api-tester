meta {
  name: Delete Expense
  type: http
  seq: 9
}

delete {
  url: {{base_url}}/api/v1/restaurants/{{restaurant_id}}/expenses/{{expense_id}}
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}
