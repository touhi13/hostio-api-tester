meta {
  name: Show Expense
  type: http
  seq: 7
}

get {
  url: {{base_url}}/api/v1/restaurants/{{restaurant_id}}/expenses/{{expense_id}}
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}
