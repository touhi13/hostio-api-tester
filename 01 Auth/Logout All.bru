meta {
  name: Logout All
  type: http
  seq: 10
}

post {
  url: {{base_url}}/api/v1/auth/logout-all
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}
