meta {
  name: Remove Party
  type: http
  seq: 7
}

delete {
  url: {{base_url}}/api/v1/restaurants/{{restaurant_id}}/waitlist/{{waitlist_id}}
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}
