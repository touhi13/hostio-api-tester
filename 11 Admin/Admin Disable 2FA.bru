meta {
  name: Admin Disable 2FA
  type: http
  seq: 28
}

post {
  url: {{base_url}}/api/v1/admin/auth/2fa/disable
  body: json
  auth: none
}

headers {
  Accept: application/json
  Authorization: Bearer {{admin_token}}
}

body:json {
  {
      "password": "{{admin_password}}"
  }
}
