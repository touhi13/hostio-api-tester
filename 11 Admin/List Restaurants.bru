meta {
  name: List Restaurants
  type: http
  seq: 7
}

get {
  url: {{base_url}}/api/v1/admin/restaurants
  body: none
  auth: bearer
}

params:query {
  search: 
  status: 
  per_page: 15
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{admin_token}}
}
