meta {
  name: Suspend Restaurant
  type: http
  seq: 9
}

post {
  url: {{base_url}}/api/v1/admin/restaurants/{{restaurant_id}}/suspend
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{admin_token}}
}
