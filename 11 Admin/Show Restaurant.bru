meta {
  name: Show Restaurant
  type: http
  seq: 8
}

get {
  url: {{base_url}}/api/v1/admin/restaurants/{{restaurant_id}}
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{admin_token}}
}
