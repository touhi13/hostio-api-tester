meta {
  name: Admin Me
  type: http
  seq: 2
}

get {
  url: {{base_url}}/api/v1/admin/auth/me
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{admin_token}}
}
