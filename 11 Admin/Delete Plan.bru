meta {
  name: Delete Plan
  type: http
  seq: 6
}

delete {
  url: {{base_url}}/api/v1/admin/plans/{{plan_id}}
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{admin_token}}
}
