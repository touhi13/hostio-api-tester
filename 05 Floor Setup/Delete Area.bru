meta {
  name: Delete Area
  type: http
  seq: 5
}

delete {
  url: {{base_url}}/api/v1/restaurants/{{restaurant_id}}/areas/{{area_id}}
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}
