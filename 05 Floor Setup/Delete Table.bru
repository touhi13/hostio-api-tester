meta {
  name: Delete Table
  type: http
  seq: 10
}

delete {
  url: {{base_url}}/api/v1/restaurants/{{restaurant_id}}/tables/{{table_id}}
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}
