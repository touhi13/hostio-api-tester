meta {
  name: Update Area
  type: http
  seq: 4
}

put {
  url: {{base_url}}/api/v1/restaurants/{{restaurant_id}}/areas/{{area_id}}
  body: json
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
      "name": "Rooftop Terrace"
  }
}
