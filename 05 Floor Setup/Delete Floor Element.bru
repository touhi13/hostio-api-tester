meta {
  name: Delete Floor Element
  type: http
  seq: 13
}

delete {
  url: {{base_url}}/api/v1/restaurants/{{restaurant_id}}/floor-elements/{{element_id}}
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}
