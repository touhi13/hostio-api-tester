meta {
  name: Step 3 - Choose Plan
  type: http
  seq: 4
}

post {
  url: {{base_url}}/api/v1/onboarding/{{restaurant_id}}/plan
  body: json
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
      "plan_id": "{{plan_id}}"
  }
}
