meta {
  name: Mark No-Show
  type: http
  seq: 6
}

post {
  url: {{base_url}}/api/v1/restaurants/{{restaurant_id}}/reservations/{{reservation_id}}/no-show
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}
